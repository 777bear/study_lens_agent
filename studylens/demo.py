from __future__ import annotations

from .models import AnalysisResult, QuizItem, SlideContent, StudyPack


def demo_result() -> AnalysisResult:
    slides = [
        SlideContent(1, "机器学习中的过拟合", "训练误差很低不代表泛化能力强；需要区分训练集、验证集和测试集。"),
        SlideContent(2, "偏差—方差权衡", "模型过于简单时高偏差；模型过于复杂时高方差。目标是在二者之间取得平衡。"),
        SlideContent(3, "正则化", "L2 正则化在损失函数中加入 λ||w||²；λ 控制拟合数据与限制参数规模的权衡。"),
        SlideContent(4, "实践方法", "交叉验证用于估计泛化误差；早停、数据增强和简化模型可缓解过拟合。"),
    ]
    pack = StudyPack(
        course_title="从过拟合到正则化：模型泛化复习讲义",
        one_sentence_summary="学会判断模型是否过拟合，并用验证策略和正则化方法提升泛化能力。",
        overview=(
            "### 学习目标\n\n"
            "理解“训练得好”与“对新数据有效”为什么不是一回事；能根据训练/验证表现判断高偏差或高方差；"
            "能解释 L2 正则化参数 λ 的作用，并选择合适的验证方法。[第1–4页]"
        ),
        background_knowledge=(
            "### 1. 经验风险与泛化\n\n"
            "**背景补充：** 训练误差是在已见样本上的平均损失，泛化误差关注未见样本。机器学习真正优化的是后者，"
            "但训练时只能借助前者及验证集近似判断。\n\n"
            "### 2. 数据集分工\n\n"
            "训练集用于学习参数；验证集用于选择超参数；测试集只在方案确定后做最终评估。[第1页]"
        ),
        knowledge_map=(
            "1. **现象**：训练好、验证差 → 可能过拟合 [第1页]\n"
            "2. **解释框架**：偏差—方差权衡 [第2页]\n"
            "3. **核心手段**：L2 正则化限制参数规模 [第3页]\n"
            "4. **验证闭环**：交叉验证 + 早停 + 数据增强 [第4页]"
        ),
        detailed_notes=(
            "## 一、如何识别过拟合\n\n"
            "如果训练误差继续下降，而验证误差停止下降甚至上升，说明模型开始记忆训练样本中的偶然噪声。"
            "关键不是看某一个误差是否“小”，而是比较两个数据分布上的表现差距。[第1页]\n\n"
            "## 二、偏差与方差\n\n"
            "高偏差表示模型表达能力不足，训练集本身也拟合不好；高方差表示模型对样本扰动敏感，训练集很好但验证集差。"
            "增加模型复杂度通常降低偏差、提高方差，因此需要验证集寻找平衡点。[第2页]\n\n"
            "## 三、L2 正则化怎么工作\n\n"
            "目标函数可写为 $J(w)=L(w)+\\lambda||w||_2^2$。第一项要求拟合数据，第二项惩罚过大的参数。"
            "λ 太小约束不足，λ 太大则模型可能欠拟合。选择 λ 时应比较交叉验证结果，而不是根据训练误差决定。[第3–4页]\n\n"
            "## 四、实践决策\n\n"
            "先画学习曲线判断问题类型；若高方差，可增加数据、数据增强、正则化、早停或简化模型；"
            "若高偏差，则应提升特征或模型能力，不能机械地继续加大正则化。[第2–4页]"
        ),
        key_points=(
            "- 训练误差低不是目标，验证/测试表现才反映泛化。[第1页]\n"
            "- 高偏差与高方差的症状和改进方向相反。[第2页]\n"
            "- λ 是超参数，应通过验证集或交叉验证选择。[第3–4页]"
        ),
        common_pitfalls=(
            "- **误区：**测试集可反复用于调参。反复查看测试结果会造成信息泄漏。\n"
            "- **误区：**正则化越强越好。过强会造成欠拟合。[第3页]\n"
            "- **核对：**课件给出的是 L2 形式，不能把 L1 的稀疏性结论直接套用。"
        ),
        review_plan=(
            "### 30 分钟速览\n\n5 分钟看知识地图；10 分钟识别两类学习曲线；10 分钟解释 λ；5 分钟完成自测。\n\n"
            "### 考前检查\n\n- 能否从训练/验证误差判断偏差或方差？\n- 能否说明 λ 增大时模型复杂度如何变化？"
        ),
        quiz=[
            QuizItem("单选", "基础", "训练误差很低而验证误差较高，最可能是什么问题？", ["A. 高偏差", "B. 高方差", "C. 数据完全正确", "D. 测试集过大"], "B", "训练集与验证集差距大是高方差/过拟合的典型表现。", ["第1–2页"]),
            QuizItem("简答", "进阶", "为什么不能用测试集反复选择 λ？", [], "这会把测试集信息泄漏到模型选择中，使最终评估过于乐观。", "测试集应只用于方案确定后的最终评估；λ 应由验证集或交叉验证选择。", ["第1页", "第4页"]),
            QuizItem("应用", "挑战", "若训练误差和验证误差都很高，应优先加大 L2 正则化吗？", [], "不应。", "这更像高偏差；继续加大正则化会进一步限制模型，应先提升特征或模型能力。", ["第2–3页"]),
        ],
        source_pages=4,
        generated_by="built-in-demo",
    )
    return AnalysisResult(slides=slides, study_pack=pack)
